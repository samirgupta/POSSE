boolean buddy_seeding_only | buddy seeding only
boolean compileSelectedOnly | compile selected only
boolean databaseReadOnly | database read only
boolean data_stats_only | data stats only
boolean filesReadOnly | files read only
boolean isEmailDisplayOnly | is email display only
boolean isReadOnly | is read only
boolean old_seeding_only | old seeding only
boolean readOnly | read only
boolean seeding_only | seeding only
boolean uploadOnly | upload only
boolean web_password_https_only | web password https only
byte FLAG_SESSION_ONLY | flag session only
byte REOP_EOLONLY | reop eol only
int ANNOUNCE_MAX_DERIVED_ONLY | announce max derived only
int CONCUR_READ_ONLY | concur read only
int DATABASE_IS_MEMORY_ONLY | database is memory only
int decipherOnly | decipher only
int encipherOnly | encipher only
int INTERVAL_INVALID_ONLY | interval invalid only
int LDT_SEEDING_ONLY | ldt seeding only
int max_upload_rate_bps_seeding_only | max upload rate bps seeding only
int MESSAGING_BT_ONLY | messaging bt only
int MODE_READ_ONLY | mode read only
int RESULTSET_FORWARD_ONLY | resultset forward only
int TEST_TYPE_DOWNLOAD_ONLY | test type download only
int TEST_TYPE_UPLOAD_ONLY | test type upload only
int TYPE_FORWARD_ONLY | type forward only
int TYPE_TEXT_ONLY | type text only
java.lang.String ICONS_ONLY | icons only
java.lang.String TEXT_ONLY | text only
java.lang.String T_ONLY | t only
long F_INFANTRY_ONLY | f infantry only
org.gudy.azureus2.plugins.ui.config.BooleanParameter remove_unauthorised_seeding_only | remove unauthorised seeding only
